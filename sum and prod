import numpy as np
# Read dimensions
n, m = map(int, input().split())

# Read the array
arr = np.array([list(map(int, input().split())) for _ in range(n)])

# Sum along axis 0, then product
result = np.prod(np.sum(arr, axis=0))

print(result)
