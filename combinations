from itertools import combinations
S, k = input().split()
k=k = int(k) # Sort string to ensure lexicographic order 
S = ''.join(sorted(S)) # Generate combinations of size 1 to k
for i in range(1, k+1): 
    for c in combinations(S, i): 
        print(''.join(c))
